# review-web Environment Variables
# Copy this file to .env.local and fill in the values

# ==================== Google Cloud Configuration ====================
# Google Cloud service account credentials for TTS and Translation
# Path to your Google Cloud service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# ==================== Content & Knowledge Management ====================
# Directory paths for content, audio, and knowledge storage
CONTENT_DIR=./content
AUDIO_ROOT=./audio
KNOWLEDGE_DIR=./knowledge

# ==================== Cloudflare R2 Configuration ====================
# Cloudflare R2 bucket configuration for audio file uploads
# Note: rclone must be configured separately via 'rclone config'
R2_BUCKET_NAME=your-bucket-name
R2_PUBLIC_URL=https://your-bucket-url.r2.dev

# ==================== Pipeline Configuration ====================
# Optional: Claude API key for social media hook generation
# Leave empty if not using Claude for social hooks
ANTHROPIC_API_KEY=your-claude-api-key-here

# ==================== GitHub Configuration (GitHub Pages + Actions) ====================
# Required for triggering GitHub Actions workflows from the web interface
# Get a personal access token from: https://github.com/settings/tokens
# Required scopes: repo, workflow
NEXT_PUBLIC_GITHUB_OWNER=YOUR_GITHUB_USERNAME
NEXT_PUBLIC_GITHUB_REPO=all-weather-protocol
NEXT_PUBLIC_GITHUB_TOKEN=ghp_your_personal_access_token_here

# Note:
# - For local development: Set these in .env.local
# - For GitHub Pages: Set NEXT_PUBLIC_GITHUB_TOKEN as a user setting (optional)
# - NEXT_PUBLIC_GITHUB_OWNER and NEXT_PUBLIC_GITHUB_REPO are auto-set in deployment

# ==================== Development ====================
# Next.js development mode
NODE_ENV=development

# Port for development server (default: 3000)
# PORT=3000
